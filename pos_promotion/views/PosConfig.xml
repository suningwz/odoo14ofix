<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record model="ir.ui.view" id="pos_config_promotion">
            <field name="name">pos.config.promotion</field>
            <field name="model">pos.config</field>
            <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//sheet" position="inside">
                    <group string="Coupon Program Automatic" groups="point_of_sale.group_pos_manager">
                        <group>
                            <field name="coupon_program_ids" widget="many2many_tags"/>
                        </group>
                    </group>
                </xpath>
            </field>
        </record>


    </data>
</odoo>